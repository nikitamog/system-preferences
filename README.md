# system-preferences

This repo is dedicated to configuration files I use when working in my own environment.

## Directory Structure

``` bash
.
├── bootstrap.sh                    # generates code from the literate config
├── init.el                         # first point of entry for emacs
├── README.md                       # this document
├── user-config.org                 # literate config file
└── user-elisp                      # directory for custom user functions
    └── nm-sys-utils.el
```
